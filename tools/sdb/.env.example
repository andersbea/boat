# Copy to tools/sdb/.env and fill in. This file (.env) is gitignored — never
# commit your real secrets.
#
# Create an OAuth client at https://dataspace.copernicus.eu
#   -> Sentinel Hub -> User settings -> OAuth clients -> Create new
SH_CLIENT_ID=your-client-id
SH_CLIENT_SECRET=your-client-secret
